# Server-side URL used by the same-origin Next.js reverse proxy.
FORGE3D_INTERNAL_API_URL=http://127.0.0.1:8000

# Optional: bypass the same-origin proxy with an explicit public API URL.
# NEXT_PUBLIC_FORGE3D_API_URL=https://POD_ID-8000.proxy.runpod.net
