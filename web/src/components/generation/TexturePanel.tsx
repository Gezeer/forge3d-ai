import { TextureStatus } from "@/types/api";
export function TexturePanel({ status, showingTextured, onGenerate, onCompare, loading }: { status?: TextureStatus | null; showingTextured: boolean; onGenerate: () => void; onCompare: (textured: boolean) => void; loading: boolean }) {
  return <section className="texture-panel"><div><span>02</span><div><b>Textura PBR</b><small>{status === "textured" ? "Textura pronta" : status === "texturing" ? "Gerando materiais e mapas..." : status === "texture_failed" ? "A textura falhou; o modelo branco foi preservado." : "Adicione materiais ao modelo branco."}</small></div></div>{status === "textured" ? <div className="compare-buttons"><button className={!showingTextured ? "active" : ""} onClick={() => onCompare(false)}>Sem textura</button><button className={showingTextured ? "active" : ""} onClick={() => onCompare(true)}>Texturizado</button></div> : <button className="texture-button" disabled={loading || status === "texturing" || status === "texture_queued"} onClick={onGenerate}>{loading || status === "texturing" ? "Texturizando..." : "Gerar textura"}</button>}</section>;
}
